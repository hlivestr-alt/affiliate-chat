'use strict';const sqlite3=require('/usr/local/lib/node_modules/n8n/node_modules/sqlite3');const db=new sqlite3.Database('/home/node/.n8n/database.sqlite',sqlite3.OPEN_READONLY);db.get("select data from execution_data where executionId=15858",(e,r)=>{const s=r.data;console.log('type',typeof s,'len',s.length,'prefix',JSON.stringify(s.slice(0,180)));console.log('hex',Buffer.from(s.slice(0,40)).toString('hex'));db.close()});
